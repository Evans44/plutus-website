// X/Twitter card image — reuses the same generated card as Open Graph so the
// preview is identical across platforms. Served at /twitter-image.
export { default, size, contentType, alt } from "./opengraph-image";
